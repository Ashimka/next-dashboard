import React from "react";

import styles from "@/styles/users/index.module.scss";

const Loading = () => {
  return (
    <>
      <div className={styles.profile__main}>Loading .....</div>
    </>
  );
};

export default Loading;
